var exercise = {};

exercise.goalCheck = function(x, y) {
    // log coordinates to the console
    console.log('x=' + x + ', y=' + y);

    // your task:
    // complete if statement used to detect
    // whether center of ball is inside goal area 1
    if ('check goal 1') {
        return true;
    }
    // your task:
    // complete if statement used to detect
    // whether center of ball is inside goal area 2
    else if ('check goal 2') {
        return true;
    }

    return false;
};

module.exports = exercise;